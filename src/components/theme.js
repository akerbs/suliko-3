import { createMuiTheme } from "@material-ui/core/styles"

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "rgba(0,0,0)",
    },
    secondary: {
      main: "rgba(249,234,207)",
    },
  },
})

export default theme

// "rgba(133,26,29)"
// rgba(249,234,207)
// rgba(43,42,41)
